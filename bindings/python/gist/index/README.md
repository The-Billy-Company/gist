# `gist.index` — trigram index lifecycle

Warmth is about speed, never correctness. This package reports and builds
gist's exact trigram index (`status` / `index`) and the capability manifest.

Relate's atlas / fragment / shelf lifecycle lives in the `relate` package
(`relate.lifecycle`).
