"""Compression as search — what resembles what, and what would explain this text.

The questions regex cannot ask. `corpus` is the substrate the rest stand on (a
corpus is a statistical population, not a `.gitignore`-filtered walk); `kinship`
answers the neighbor and repetition questions; `retrieval` prices text you
already have against the tree; `sweep` runs N exact patterns in one attributed
walk.

Every distance, gain, and grade is the kernel's. This package carries them
across the transport with their provenance intact and never re-derives one.
"""

from __future__ import annotations
